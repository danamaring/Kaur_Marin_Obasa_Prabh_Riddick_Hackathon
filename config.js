const config = {
    uname: 'root',
    upass: 'root', //leave it blank for windows
    port: 8889, //windows 3306
    host: 'localhost',
    db: 'biscard'
}

// make this a public file our app can read
module.exports = config;